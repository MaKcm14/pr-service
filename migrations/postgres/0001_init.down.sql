-- Delete the relation for the connecting relation.
DROP TABLE IF EXISTS assigned_reviewers;

-- Delete the relation for the models 'Pull request'.
DROP TABLE IF EXISTS pull_requests;

-- Delete the relation for the models 'User'.
DROP TABLE IF EXISTS users;

-- Delete the relation for the models 'Team'.
DROP TABLE IF EXISTS teams;
